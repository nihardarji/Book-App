# MERN stack Book Application with GraphQL


### Add `.env` file to root directory
#### Add `MONGO_URI` & `PORT` to `.env` file

## Available Scripts
### In the project directory, you can run:

### `npm run app-install`
To install all the packages in both frontend and backend

### `npm run dev`

Runs the app in the development mode.\
Open [http://localhost:3000](http://localhost:3000) to view it in the browser.
